apiVersion: v1
kind: Service
metadata:
  name: otus-maas
  namespace: otus-maas
  labels:
    app: otus-maas
spec:
  type: ClusterIP
  selector:
    app: otus-maas
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
